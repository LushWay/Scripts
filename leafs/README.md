# Leafs

Leafs is a scripts that uses leafy-utils package to generate files, autocommiting or any other cool stuff. Also, leafy-utils automatically adds them to package.json/scrips for better experience.
